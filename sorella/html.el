;; ----------------------------------------------------------------------------
;; HTML/SGML/XML stuff
;; ----------------------------------------------------------------------------
(setq sgml-basic-offset tab-width) ;; honour the default tab width

(add-hook 'sgml-mode-hook 'zencoding-mode)
